<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <IsPackable>true</IsPackable>
    <PackageId>Gleanvolt.Core</PackageId>
    <Description>Domain models, control strategies and hardware abstractions for the Gleanvolt. No hardware, hosting or framework dependencies -- the decision logic on its own.</Description>
  </PropertyGroup>

</Project>
